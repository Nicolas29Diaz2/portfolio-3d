/** Time to animate the intro camera movement (ms). */
export const introAnimateCamera = 2000

/** Time to show the start text and button after the progress bar is full (ms). */
export const showStartTextTime = 1000

/** Time to slide in the loading screen after the logo disappears (ms). */
export const slideInTime = 0

/** Time to show and enable the screens after the start button is pressed (ms). */
export const showScreensTime = 500

/** Time to start the intro animation (ms). */
export const introAnimationTime = 0

/** Time to stop fixedMoveCameraToObject and show the cancel button (ms). */
export const fixedMoveCameraToObjectTime = 1000

/** Time to enable camera movement after pressing the start button (ms). */
export const enableCameraMovementTime = 1000

/** Time to show float buttons after pressing the start button (ms). */
export const showFloatButtonsTime = 2000

/** Character sit animation speed multiplier. */
export const characterSitSpeed = 2.5

/** Character intro animation speed multiplier. */
export const characterIntroSpeed = 1.2

/** Delay before starting the dissolve-eyes animation (ms). */
export const characterEyesDissolveDelay = 2100

/** Delay before starting the helmet animation (ms). */
export const helmetAnimationDelay = 500

/** Helmet animation speed multiplier. */
export const helmetAnimationSpeed = 2

/** Dissolve-eyes animation speed (0.01 – 1). */
export const dissolveEyesSpeed = 0.05

/** Time before showing the move-scene tutorial icon (ms). */
export const showIconTutorialCharacterTime = 2100

/** Time before hiding the move-scene tutorial icon (ms). */
export const hideIconTutorialCharacterTime = 4500

/** Time before hiding the skills tutorial icon (ms). */
export const hideSkillIconTutorialTime = 2500

/** Time before hiding the projects tutorial icon (ms). */
export const hideProjectsIconTutorialTime = 2500
